syntax = "proto3";

package grtsproto;

/* Server -> Client */
message Connected {
    uint32      playerId = 1;
    string      playerLogin = 2;
}

message JoinedGame {
    uint32      gameId = 2;
}

/* Client -> Server */
message ChangeName {
    string      name = 2;
}
